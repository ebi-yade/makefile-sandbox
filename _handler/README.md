# Contribution Docs for Handlers

You might get want to write something like documentation or place any other files in this directory.  But don't worry: the command `make handlers` works even if in that case!
